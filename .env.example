# Copiar a `.env` (no se versiona) y completar.

# Endpoint que devuelve las filas de dbo.SDP_V_TIEMPOREAL_GOM en JSON.
# Vacío = el visor arranca con datos de ejemplo.
VITE_PEOPLE_API_URL=

# Endpoint que devuelve el catálogo de proyectos/sectores/planos en JSON.
# Vacío = se usa PROJECTS de src/data/source.ts.
VITE_PLANS_URL=
